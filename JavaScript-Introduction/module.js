let Airplane = {};
Airplane.myAirplane = "StarJet";

module.exports = Airplane;


let Menu = {}; //creates Object that represents the module Menu

let Plane = {}; // object module? 

Menu.specialty = "roasted beef marinated in skittles sauce" // defined as a property of the Menu module 
// add data to the menu object by setting properties on that object and a value to those properties

module.exports = Menu; //exports the Menu object as a module. is a variable that represents the module.
//export exposes the module as an object


//define an object to represent the module 
// add data and behaviour to the module
// export the module 